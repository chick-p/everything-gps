# everything-gps(shortcut)

## Development

Install dependencies:

```shell
pnpm install
```

Preview site:

```shell
pnpm run dev
```

## Deploy

Deploy app to Cloudflare Workers:

```shell
pnpm run deploy
```
